kprobe:io_req_cqe_overflow
{
		@counts["io_req_cqe_overflow", nsecs / 1000000] = count();
}

uprobe:/home/jhlee/io_uring-echo-server/pingpong-s/napi-busy-poll-server:io_uring_sqe_overflow
{
		@counts["io_uring_sqe_overflow", nsecs / 1000000] = count();
}

interval:s:1
{
		printf("function,tick,count\n");
		print(@counts);
		clear(@counts);
}
